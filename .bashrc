
# add the code below to the home directory .bashrc .
# then each time a new terminal is opened in this directory,
# conda environment is automatically activated.

# if [ -f $PWD/.bashrc ] && [ $BASH_SOURCE != $PWD/.bashrc ]
# then
#     source $PWD/.bashrc
# fi



conda activate al
